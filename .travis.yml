language: minimal
dist: bionic

services:
  - docker

before_install:
  - docker build -t mpinese/nimpress .
  - docker run --entrypoint nimble mpinese/nimpress testfail

# script:
#   echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

# deploy:
#   provider: script
#   script: bash docker_push
#   on:
#     branch: master
